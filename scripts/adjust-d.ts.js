// copied from https://github.com/tweenjs/tween.js
/**
 * Make adjustments to the definition generated by Typescript
 */

var fs = require('fs');
var path = require('path');

var file = path.join(__dirname, '../dist/tween.d.ts');

let content = fs.readFileSync(file) + '';

content = content
  .replace(/\r\n/g, '\n')
  .replace(/\s*export default [^;{]+;/g, '')
  .replace(/export {.*};/gs, '')
  .replace(/export (default )?/g, '')
  .replace(/(}\n)?declare module [^{]+{/g, '')
  .replace(/import [^;]+;/g, '')
  .replace(/}\n$/, '')
  .replace(/;\n(\s*)\//g, ';\n\n$1/')
  .replace(/}\n(\s*)([^\s])/g, '}\n\n$1$2')
  .replace(/\n\n(\s*\n)+/g, '\n\n');

content = `declare module "tween-animate" {
    import EventEmitter from 'eventemitter3';
${content}
    export {
      Easing,
      Animate,
      Transform,
      update
    };
}
`;

fs.writeFileSync(file, content);
